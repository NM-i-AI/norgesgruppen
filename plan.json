{
  "session_id": "2026-03-20-23-26",
  "created_at": "2026-03-20T23:26:58.952830+00:00",
  "revision": 2,
  "current_step": 4,
  "plan": [
    {
      "step": 1,
      "type": "exploration",
      "title": "Verify environment and package versions",
      "hypothesis": null,
      "description": "Check installed versions of ultralytics, torch, torchvision, timm, pycocotools, ensemble-boxes. Install/pin ultralytics==8.1.0 if needed. Confirm GPU availability and VRAM.",
      "success_criteria": "All critical packages at correct versions, GPU confirmed with VRAM reported",
      "status": "completed",
      "experiment_id": "exp-001"
    },
    {
      "step": 2,
      "type": "exploration",
      "title": "Deep dive into dataset structure, annotations, and metadata",
      "hypothesis": "Understanding the full dataset structure will inform model design choices",
      "description": "Load data/train/annotations.json: count images, annotations, categories. Compute stats: annotations per image (min/max/mean), category frequency distribution, image dimensions. Examine bbox format, area, corrected flag, iscrowd. Identify the store sections from image filenames or metadata. Check category_id distribution \u2014 which categories are rare vs common. Check category 356 (unknown_product). Load metadata.json and understand its schema. Examine data/products/ structure (how many products have reference images, how many images per product, image sizes).",
      "success_criteria": "Complete understanding of dataset: number of images, annotations, categories, image sizes, annotation density, category distribution, reference image availability",
      "status": "completed",
      "experiment_id": "exp-002"
    },
    {
      "step": 3,
      "type": "refactor",
      "title": "Create train/val split, evaluation function, and YOLO data conversion",
      "hypothesis": "A proper evaluation setup is essential before any training",
      "description": "Install pycocotools, scikit-learn, ensemble-boxes. Create image-level 90/10 train/val split (seed=42), ensuring category coverage. Write COCO-format split JSONs. Implement eval function computing: detection_mAP@0.5 (all preds mapped to single class) and classification_mAP@0.5 (original category_ids), combined as val_score = 0.7*det + 0.3*cls. Convert COCO annotations to YOLO txt format for ultralytics (both nc=356 multi-class and nc=1 single-class versions). Create dataset YAML files. Verify with a dummy test.",
      "success_criteria": "Split files created, eval function returns sensible scores on dummy data, YOLO format files verified",
      "status": "completed",
      "experiment_id": "exp-003"
    },
    {
      "step": 4,
      "type": "experiment",
      "title": "Baseline: YOLOv8m nc=356 at 1280px",
      "hypothesis": "YOLOv8m at 1280px establishes a solid multi-class baseline for this dense detection task",
      "description": "Train YOLOv8m on train split with nc=356, imgsz=1280, 100 epochs, batch=16, close_mosaic=20. Run inference on val split, evaluate with eval function. This gives us both detection and classification baselines.",
      "success_criteria": "Training completes, val_score > 0.1 (establishing a non-trivial baseline)",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 5,
      "type": "experiment",
      "title": "YOLOv8x nc=1 detection-only at 1280px",
      "hypothesis": "Single-class detector achieves higher detection mAP since it only needs to localize products without distinguishing 356 classes",
      "description": "Train YOLOv8x nc=1 on single-class train split, imgsz=1280, 200 epochs, batch=8, close_mosaic=50. Evaluate detection_mAP@0.5 on val split. This will be the detection backbone for the two-stage approach.",
      "success_criteria": "detection_mAP@0.5 exceeds the nc=356 baseline's detection mAP by at least 0.05",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 6,
      "type": "exploration",
      "title": "Explore DINOv2 embeddings for product classification",
      "hypothesis": "DINOv2 embeddings can distinguish between product categories using reference images and training crops",
      "description": "Load DINOv2-base (or large if memory allows). Extract embeddings for: (a) cropped training annotations (sample per category), (b) reference product images from data/products/. Build a gallery. Measure kNN classification accuracy on val GT boxes with different k values and gallery compositions (reference only, crops only, mixed). This determines if two-stage is viable.",
      "success_criteria": "kNN classification accuracy on val GT boxes > 50% top-1",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 7,
      "type": "experiment",
      "title": "Two-stage: nc=1 YOLO + DINOv2 kNN classifier",
      "hypothesis": "Two-stage approach achieves higher val_score than end-to-end nc=356 YOLO",
      "description": "Use nc=1 YOLO detections on val split. For each detection, crop from original image, extract DINOv2 embedding, kNN lookup against gallery. Evaluate full val_score (0.7*det + 0.3*cls). Compare to multi-class YOLO baseline.",
      "success_criteria": "val_score exceeds the nc=356 YOLO baseline",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 8,
      "type": "experiment",
      "title": "SAHI-style tiled inference for dense shelf detection",
      "hypothesis": "Tiled inference improves detection of small/dense products by 3-5 mAP points",
      "description": "Implement tiled inference: slice images into overlapping tiles (e.g., 1280x1280 with stride 960), run YOLO on each tile + full image, merge with NMS/WBF. Evaluate on val split.",
      "success_criteria": "detection_mAP@0.5 improves by at least 0.02 over non-tiled inference",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 9,
      "type": "experiment",
      "title": "YOLOv8x nc=356 at 1280px, 200 epochs",
      "hypothesis": "YOLOv8x multi-class may compete with two-stage if given enough capacity and training time",
      "description": "Train YOLOv8x with nc=356, imgsz=1280, 200 epochs, batch=8, close_mosaic=50. Compare val_score to both baseline and two-stage approach. This determines if end-to-end or two-stage is better.",
      "success_criteria": "val_score comparison with two-stage approach is informative for final architecture choice",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 10,
      "type": "experiment",
      "title": "NMS/confidence threshold tuning and TTA",
      "hypothesis": "Tuning post-processing improves val_score by 1-3 points",
      "description": "Using best detector so far, sweep NMS IoU threshold {0.4-0.8}, confidence threshold {0.05-0.3}, and test TTA (augment=True). Find optimal combination on val split.",
      "success_criteria": "val_score improves by at least 0.01 over default thresholds",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 11,
      "type": "experiment",
      "title": "Fine-tune DINOv2 projection head with ArcFace/SupCon loss",
      "hypothesis": "Learned projection head improves classification mAP by 2-5 points over raw embeddings",
      "description": "Freeze DINOv2, add 2-layer MLP projection head. Train with ArcFace loss on training crops + reference images. Evaluate classification improvement on val split with GT boxes, then with detected boxes.",
      "success_criteria": "Classification accuracy improves by at least 2 points over raw kNN",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 12,
      "type": "experiment",
      "title": "Confidence gating: YOLO classes vs DINOv2",
      "hypothesis": "Using YOLO class when confident and DINOv2 when uncertain improves classification",
      "description": "For each detection, if YOLOv8x nc=356 confidence > threshold use YOLO class, else use DINOv2 kNN. Sweep threshold on val split. This combines strengths of both approaches.",
      "success_criteria": "val_score exceeds both pure YOLO and pure DINOv2 approaches",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 13,
      "type": "experiment",
      "title": "Extended training and model ensemble",
      "hypothesis": "Longer training and/or ensembling YOLO variants with WBF improves detection",
      "description": "Train best YOLO config for 400 epochs with tuned augmentation. Optionally train RT-DETR-l nc=1 and ensemble with WBF. Evaluate detection improvement.",
      "success_criteria": "detection_mAP improves by at least 0.02 over single model",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 14,
      "type": "experiment",
      "title": "Optimal model combination for 420MB budget",
      "hypothesis": "The right combination of detector + classifier within size constraints maximizes val_score",
      "description": "Enumerate feasible combinations considering 420MB limit: (a) YOLOv8x FP16 + DINOv2-base FP16, (b) YOLOv8l FP16 + DINOv2-large FP16, (c) ensemble INT8 + DINOv2-base FP16. Test top combinations on val split. Check total sizes.",
      "success_criteria": "Best combination identified that fits in 420MB and maximizes val_score",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 15,
      "type": "refactor",
      "title": "Build complete inference pipeline (run.py)",
      "hypothesis": "End-to-end pipeline reproduces val_score",
      "description": "Assemble best components into run.py: tiled inference (if beneficial), best detector, best classifier, optimal thresholds. Test end-to-end on val split. Profile inference time.",
      "success_criteria": "run.py reproduces val_score within 0.5% and runs within time limits",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 16,
      "type": "experiment",
      "title": "Final ablation and sanity check",
      "hypothesis": "Each component contributes positively to final val_score",
      "description": "Run ablation: remove each component one at a time and measure val_score drop. Confirm every component is net positive. Run full pipeline on val split as final verification.",
      "success_criteria": "All components confirmed positive, final val_score matches expectations",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 17,
      "type": "refactor",
      "title": "Package final submission",
      "hypothesis": "Submission meets all constraints",
      "description": "Create submission.zip with run.py, model weights, gallery files, helper scripts. Verify: total size < 420MB, no blocked imports, correct output format. Test run.py with --input and --output args.",
      "success_criteria": "Submission passes all format checks and size constraints",
      "status": "pending",
      "experiment_id": null
    }
  ]
}