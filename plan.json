{
  "session_id": "2026-03-20-11-51",
  "created_at": "2026-03-20T11:51:33.547028+00:00",
  "revision": 1,
  "current_step": 4,
  "plan": [
    {
      "step": 1,
      "type": "exploration",
      "title": "Explore dataset structure and statistics",
      "hypothesis": null,
      "description": "Write scratch.py to load annotations.json and metadata.json. Print: number of images, annotations, categories, annotation distribution per image (min/max/mean/median), category frequency distribution, how many categories have <5 annotations, image sizes, and verify file paths exist for a sample of train images and product reference images. Also check which product_codes in annotations have matching folders in data/products/.",
      "success_criteria": "Complete statistical summary of the dataset logged, understanding of class distribution and data linkage",
      "status": "completed",
      "experiment_id": "exp-001"
    },
    {
      "step": 2,
      "type": "experiment",
      "title": "Single-class YOLOv8m detection baseline at 1280px",
      "hypothesis": "A single-class YOLOv8m detector at 1280px trained for 50 epochs will achieve detection_mAP@0.5 > 0.60, giving final_score > 0.42 (with classification=0)",
      "description": "Fix the environment by ensuring ultralytics is properly imported. Convert COCO annotations to YOLO format with ALL categories mapped to class 0 (single-class detection). Split 80/20 train/val. Train YOLOv8m at imgsz=1280 for 50 epochs with batch=8. Evaluate detection mAP@0.5 on val set using pycocotools. This establishes a working baseline and validates the full pipeline. Key changes to main.py: simplify to single-class, use YOLOv8m (not l), reduce epochs to 50 for faster iteration.",
      "success_criteria": "Training completes, detection_mAP@0.5 > 0.50, pipeline produces valid predictions",
      "status": "completed",
      "experiment_id": "exp-002"
    },
    {
      "step": 3,
      "type": "experiment",
      "title": "Multi-class YOLOv8m detection at 1280px",
      "hypothesis": "Training with all 356 categories enables both detection and classification scoring, achieving final_score > 0.50 (0.7*det + 0.3*cls)",
      "description": "Modify the baseline to train with all 356 categories instead of single-class. Use YOLOv8m at 1280px for 80 epochs. The YOLO model will learn both localization and classification jointly. Evaluate both detection mAP@0.5 (category-agnostic) and classification mAP@0.5 (category-aware). Compare final_score against single-class baseline.",
      "success_criteria": "final_score > 0.45, classification_mAP@0.5 > 0.10",
      "status": "completed",
      "experiment_id": "exp-003"
    },
    {
      "step": 4,
      "type": "experiment",
      "title": "Scale up to YOLOv8l with augmentation tuning",
      "hypothesis": "YOLOv8l with copy-paste augmentation and tuned hyperparameters improves both detection and classification over YOLOv8m",
      "description": "Train YOLOv8l at 1280px for 100 epochs with batch=4. Enable copy-paste=0.3, mosaic=1.0, mixup=0.1. Use cosine LR schedule. Compare against YOLOv8m results. If detection improves but classification is still weak, this motivates a two-stage approach.",
      "success_criteria": "final_score improvement of at least 0.03 over step 3",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 5,
      "type": "experiment",
      "title": "Two-stage: YOLO detection + DINOv2/CLIP crop classification",
      "hypothesis": "Using a strong single-class detector + embedding-based classification of crops against reference images will significantly boost classification mAP for rare classes",
      "description": "Take the best single-class detector. For each detected box, crop the region and extract embeddings using a pretrained vision model (try torchvision ResNet50 or similar available model). Extract embeddings for all product reference images (data/products/*/main.jpg). Classify each crop by nearest-neighbor matching. Evaluate classification mAP improvement. This leverages the ~335 products with reference images.",
      "success_criteria": "classification_mAP@0.5 > 0.15, final_score > previous best",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 6,
      "type": "experiment",
      "title": "SAHI tiled inference for dense small objects",
      "hypothesis": "Sliced inference on high-res images improves detection of small/dense products, boosting detection_mAP@0.5 by 0.05+",
      "description": "Implement sliced inference: divide each image into overlapping 640x640 tiles, run detection on each tile, merge with NMS. Apply to the best detection model. Compare detection mAP against full-image inference.",
      "success_criteria": "detection_mAP@0.5 improves by at least 0.03",
      "status": "pending",
      "experiment_id": null
    },
    {
      "step": 7,
      "type": "experiment",
      "title": "Ensemble best approaches and optimize for submission",
      "hypothesis": "Combining multi-class YOLO predictions with two-stage crop classification and SAHI detection maximizes final_score",
      "description": "Assemble the best detection method (SAHI or full-image) with the best classification method (YOLO multi-class or crop-based retrieval). Use WBF to merge detection ensembles if beneficial. Tune confidence thresholds. Profile inference time to ensure <300s on L4 GPU. Generate final submission.",
      "success_criteria": "final_score > 0.55, inference completes within 300s",
      "status": "pending",
      "experiment_id": null
    }
  ]
}